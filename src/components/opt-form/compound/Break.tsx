import React from 'react'
// Styled
import { Break } from '../styles/opt-form'

const OptFormBreak = ({ ...restProps }): JSX.Element => {
  return <Break {...restProps} />
}

export default OptFormBreak
