import React from 'react'
import { PlayButton } from '../styles/header'

const HeaderPlayButton: React.FC = ({ children }) => {
  return <PlayButton>{children}</PlayButton>
}

export default HeaderPlayButton
